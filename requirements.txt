# CPU-only PyTorch builds — much smaller download/install than the default
# CUDA-enabled wheels from PyPI, and the only thing available on Streamlit
# Community Cloud's free tier anyway.
--extra-index-url https://download.pytorch.org/whl/cpu

streamlit>=1.58.0
torch==2.9.0
torchvision==0.24.0
numpy>=1.24.0
pandas>=2.0.0
plotly>=5.18.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
Pillow>=10.0.0
streamlit-pdf-viewer>=0.0.29

# Optional — Quality Assistant upgrades (gated by try/except in core/rag.py,
# the app works fine without them). Left disabled here to keep the deployed
# build small/fast; uncomment locally if you want them.
# anthropic>=0.25.0             # enables Claude-generated answers if ANTHROPIC_API_KEY is set
# sentence-transformers>=2.2.0  # enables semantic (vs TF-IDF) retrieval
